role_id: resource_builder
name: Resource Builder Agent
profile_markdown: docs/agents/resource-builder-agent-profile.md

model:
  provider: openai
  model_name: gpt-5.4
  temperature: 0.2

memory:
  type: openviking
  namespace: agents/resource_builder/memory

context:
  roots:
    - agents/resource_builder/profile
    - bundles/psca/resource-guidance

tools:
  allowed:
    - artifact_reader
    - artifact_writer

output:
  primary_artifact: ResourceTaskPacket

behavior:
  narrate_progress: false
  expose_delegation_trace: false