role_id: requirements
name: Requirements Agent
profile_markdown: docs/agents/requirements-agent-profile.md

model:
  provider: openai
  model_name: gpt-5.4
  temperature: 0.2

memory:
  type: openviking
  namespace: agents/requirements/memory

context:
  roots:
    - agents/requirements/profile
    - bundles/psca/clinical-guidance
    - bundles/psca/requirements

tools:
  allowed:
    - terminology_lookup
    - artifact_reader

output:
  primary_artifact: PatientSummarySpecification

behavior:
  narrate_progress: false
  expose_delegation_trace: false